import { loginData } from "./loginData";

export const testData = {
    standardUser: {
        accountLogin: loginData.standardUser
    },
    lockedOutUser: {
        accountLogin: loginData.lockedOutUser
    },
    problemUser: {
        accountLogin: loginData.problemUser
    }
}